@{
    ViewBag.Title = "Reset password";
}
@model Uma.Eservices.Models.Account.ResetPasswordViewModel
<div class="row">
    <div class="path-descr">
        <div>@T("Home")</div>
        <div>@T("Reset password")</div>
    </div>
</div>

<h1>@T(ViewBag.Title)</h1>
<div class="row">
    <div class="col-sm-8">
        @using (Html.BeginForm(MVC.Account.ActionNames.ResetPassword, MVC.Account.Name, FormMethod.Post, new { @class = "form-horizontal" }))
        {
            @Html.AntiForgeryToken()
            @Html.HiddenFor(m => m.Code)
            <div class="row form-row">
                <div class="col-sm-4 col-sm-offset-2">
                    <div class="form-group" style="text-align: left">
                        @Html.UmaLabelFor(m => m.EmailAddress)
                        @Html.UmaTextBoxFor(m => m.EmailAddress)
                        @Html.ValidationMessageFor(m => m.EmailAddress)
                    </div>
                </div>
            </div>

            <div class="row form-row">
                <div class="col-sm-4 col-sm-offset-2">
                    <div class="form-group" style="text-align: left">
                        @Html.UmaLabelFor(m => m.Password)
                        @Html.PasswordFor(m => m.Password, new { @class = "form-control" })
                        @Html.ValidationMessageFor(m => m.Password)
                    </div>
                </div>
            </div>

            <div class="row form-row">
                <div class="col-sm-4 col-sm-offset-2">
                    <div class="form-group" style="text-align: left">
                        @Html.UmaLabelFor(m => m.ConfirmPassword)
                        @Html.PasswordFor(m => m.ConfirmPassword, new { @class = "form-control" })
                        @Html.ValidationMessageFor(m => m.ConfirmPassword)
                    </div>
                </div>
            </div>

            <div class="row form-row" style="padding-top: 30px;">
                <div class="col-sm-4 col-sm-offset-2">
                    @Html.UmaButton("submitButton", @T("RESET").ToString(), UmaButtonType.FormSubmit, null, new { @class = "btn-block" })
                </div>
            </div>
        }
    </div>
</div>
